# Gmail MCP server — community server; PENDING per Part 4 §4.6
# Generate OAuth client: https://console.cloud.google.com/apis/credentials
# Redirect URI must match (e.g. http://localhost:3000/oauth2callback)
GMAIL_CLIENT_ID=REPLACE_ME
GMAIL_CLIENT_SECRET=REPLACE_ME
GMAIL_REDIRECT_URI=http://localhost:3000/oauth2callback
